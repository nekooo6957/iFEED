# GameDesign V2 方案计划（四关制、库存解谜、保留抛投）

## Summary
1. 新增文档 [GameDesignV2.md](/f:/cluade_code/FEEMgame/GameDesignV2.md)，保留旧版 [GameDesign.md](/f:/cluade_code/FEEMgame/GameDesign.md) 作为历史版本。
2. V2 核心改为“饥饿值 + 固定库存 + 可解性生成算法”，同时保留当前抛投命中操作与碰撞判定框架。
3. 规则按你已确认的口径固化：4关、全动物需喂饱、总饥饿点进度、单关广告总次数=1、无倒计时、无积分。

## Key Changes
1. 玩法与胜负规则重写。
   - 动物：青蛙/小鸡/小狗/羊/乌龟/小猫/兔子/鱼。
   - 食物：胡萝卜/虫子/肉骨头/青菜/虾/饲料。
   - 饥饿值：L1固定1，L2随机1~2，L3-L4随机1~3。
   - 进度显示：左上角显示 `关卡` + `已消除饥饿值/总饥饿值`（分母为本局动态总和）。
   - 通关：所有动物饥饿值 `<=0`。
   - 失败：食物耗尽且仍有未喂饱动物，或进入不可解死局（无可用操作且广告机会已用）。

2. 关卡结构与难度重做。
   - L1：中间1只（教程）。
   - L2：前中后各1只（共3只，低计算压力）。
   - L3：3x3（9只，高策略）。
   - L4：4x4（16只，最高策略）。
   - 动物逐关解锁采用“复杂度解锁”。
   - 食物逐关解锁，前两关简化，后两关开放全量。
   - 库存冗余按“前多后少”：L1约+50%、L2约+30%、L3约+10%、L4约0~5%。
   - 可放少量干扰食物，但必须满足“无广告可通关”。

3. 食物效果矩阵写入规范表。
   - 每种动物只允许吃“可减饥饿值食物”。
   - 吃非允许食物：进入生病状态。
   - 生病状态：普通投喂无效且消耗库存。
   - 混合喂养允许（同一动物可用多种可食物叠加减值）。
   - 一次抛投只结算一只动物（命中最近目标）。

4. 道具与广告规则重写。
   - 救治针：看广告后恢复1只生病动物。
   - 万能饼干：看广告后可直接喂饱任意1只动物（含生病）。
   - 单关广告总次数上限=1（救治针/万能饼干二选一）。
   - 广告道具仅补救失误，不作为关卡必需条件。

5. 可解性算法章节（含伪代码与复杂度）新增。
   - 生成流程：先生成动物与饥饿值，再按动物可食谱构造基础可行喂养方案，再叠加关卡冗余与少量干扰。
   - 校验流程：使用带记忆化的回溯/DFS验证“无广告可通关”。
   - 推荐伪代码函数：`generateLevel()`, `buildBaseFeedingPlan()`, `isSolvableWithoutAds(state)`。
   - 复杂度说明：验证最坏指数级，但在 `<=16只动物、每只饥饿<=3` 且启发式剪枝下可控；生成失败则重采样（如最多50次）。

6. 与现有代码接口对齐（文档中给出迁移目标）。
   - `FoodType` 改为6种新食物集合。
   - `AnimalType` 改为8种新动物集合。
   - `AnimalConfig` 从 `preferredFood/deathFood` 改为 `foodEffects: Partial<Record<FoodType, 1|2>>`。
   - `AnimalEntity` 增加 `hungerMax/hungerCurrent/status(sick|hungry|full)`，移除 `requiredFeeds/currentFeeds` 依赖。
   - `GameState` 增加 `foodInventory`、`totalHunger`、`clearedHunger`、`adUsedThisLevel`、`sickCount` 等字段，移除旧 `combo/score/timeLeft` 作为主目标字段。

## Test Plan
1. 规则单测：8种动物仅对配置食物减值，非配置食物必生病。
2. 状态单测：生病动物普通投喂无效且扣库存；救治针恢复后可正常喂养；万能饼干可直接喂饱生病/健康动物。
3. 进度单测：`clearedHunger/totalHunger` 实时正确，通关仅在全部动物 `hunger<=0` 时触发。
4. 生成算法单测：每关随机种子下库存均存在至少一条“无广告通关路径”。
5. 难度验收：L1/L2明显易于L3/L4，且L4接近低冗余高策略目标。
6. 交互验收：保留抛投命中手感与单次命中单目标结算，不回退为点选喂食。

## Assumptions
1. V2 文档采用中文为主并保持旧文档的章节风格（含中英小标题）。
2. 第4关通过后进入最终通关结算页，不进入无尽模式。
3. 结果页不展示分数，仅展示通关/失败与关键原因。
4. 若出现“无可用食物 + 未喂饱动物”或“不可逆死局”，立即判负并给出原因提示。
